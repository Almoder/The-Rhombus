<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/thresholdBackground"
    tools:context=".Threshold">
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/thresholdCaption"
                android:textSize="@dimen/thresholdCaptionTextSize"
                android:textColor="@color/thresholdCaptionText"
                android:layout_marginBottom="@dimen/thresholdCaptionMarginBottom" />
            <Button
                android:id="@+id/startButton"
                android:background="@color/thresholdStartButtonBack"
                android:padding="@dimen/thresholdStartButtonPadding"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/thresholdStartButton"
                android:textSize="@dimen/thresholdStartButtonTextSize"
                android:textColor="@color/thresholdStartButtonText"
                android:onClick="startButtonClick"/>
        </LinearLayout>
    </androidx.core.widget.NestedScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>