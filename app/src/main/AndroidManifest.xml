<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="space.almoder.therhombus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Game">
        <activity android:name=".shop.ShopActivity"></activity>
        <activity
            android:name=".Splash"
            android:theme="@style/splash"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlayerImages" />
        <activity android:name=".CustomGame" />
        <activity
            android:name=".Threshold"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".Authorise" />
        <activity android:name=".MainMenu" />
        <activity android:name=".Campaign" />
        <activity android:name=".Settings" />
        <activity
            android:name=".Game"
            android:screenOrientation="portrait" />
    </application>

</manifest>